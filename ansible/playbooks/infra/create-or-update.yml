---
- name: Create or update infrastructure
  hosts: localhost
  become: false
  gather_facts: false

  tasks:
  - name: Run terraform apply
    include_role:
      name: terraform
      tasks_from: apply
    vars:
      terraform_apply_state: "present"
      target_module: "aws-vm-{{ environment }}"
